# RunPod SDK
runpod==1.6.0


# Torch + CUDA 11.7 wheels
--extra-index-url https://download.pytorch.org/whl/cu117
torch==1.13.1+cu117
torchvision==0.14.1+cu117


# Core libs
numpy==1.24.4
opencv-python==4.9.0.80
scipy==1.10.1
scikit-image==0.22.0
face-alignment==1.3.5
facenet-pytorch==2.5.3
tqdm==4.66.4
fvcore==0.1.5.post20221221
iopath==0.1.10
trimesh==4.4.0


# Build PyTorch3D from source (stable branch)
# Note: this builds during image build, which avoids first-request latency.
# If a prebuilt wheel exists for your exact env, you can switch to it.
# Keep this at the bottom so earlier deps are present.
PyYAML==6.0.2
"git+https://github.com/facebookresearch/pytorch3d.git@stable"